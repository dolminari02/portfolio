# CLAUDE.md

Claude Code가 이 프로젝트에서 작업할 때 따르는 규칙입니다.

---

## 프로젝트 정보

- **프로젝트**: 개인 포트폴리오 웹사이트 — React 학습을 겸한 개인 프로젝트
- **성격**: 학습 우선. 단순히 완성하는 게 목표가 아니라, 만드는 과정에서 React를 이해하는 게 목표다.
- **기술 스택**: Next.js (App Router) + Tailwind CSS / JavaScript
- **배포**: GitHub Pages (정적 내보내기 방식)

### 주요 명령
- **설치**: `npm install`
- **개발 서버**: `npm run dev` → 브라우저에서 `http://localhost:3000`
- **프로덕션 빌드**: `npm run build` (정적 파일이 `out/` 폴더에 생성됨)
- **배포**: GitHub 저장소에 `git push` → GitHub Actions가 자동으로 빌드·배포
  - ⚠️ 이 자동 배포는 최초 1회 설정이 필요하다 (아래 "배포 설정 주의" 참고)

### 디렉터리 핵심
- `app/` — 페이지와 레이아웃 (파일 기반 라우팅)
- `components/` — 재사용 컴포넌트
- `public/` — 이미지 등 정적 파일
- `out/` — 빌드 결과물 (직접 수정하지 않음)

### 컨벤션
- 컴포넌트 파일명은 PascalCase (예: `Header.jsx`)
- 커밋 메시지는 한글 명령형 (예: "헤더 컴포넌트 추가")
- 스타일링은 Tailwind 유틸리티 클래스만 사용 (별도 CSS 파일 최소화)

### 절대 건드리지 말 것
- `.env` 등 시크릿 파일
- `node_modules/` (OneDrive 동기화 충돌 주의 — 문제 생기면 동기화 제외)
- `out/` 빌드 산출물

### 배포 설정 주의 (Next.js + GitHub Pages)
- GitHub Pages는 정적 파일만 서빙하므로 `next.config.js`에 `output: 'export'`가 필요하다.
- 프로젝트 저장소로 배포할 경우 `basePath`와 이미지 `unoptimized: true` 설정이 필요할 수 있다.
- 이 설정을 임의로 바꾸기 전에 반드시 사용자에게 이유를 설명하고 확인받는다.

---

## 학습 모드 (이 프로젝트의 최우선 규칙)

- 코드를 작성하거나 수정하기 전에, **무엇을 왜 그렇게 하는지 먼저 짧게 설명**한다. "그냥 이렇게 하면 돼"가 아니라 이유를 함께 준다.
- 새로운 React 개념(컴포넌트, props, state, hook 등)이 처음 등장하면 한두 문장으로 개념을 짚어준다.
- 한 번에 너무 많은 걸 쏟지 않는다. 단계별로 진행하고, 각 단계가 이해됐는지 확인하며 다음으로 넘어간다.
- 사용자는 JavaScript 기초는 있지만 React는 처음이다. 이 수준에 맞춰 설명한다.

---

## 작업 원칙

- 여러 단계가 필요한 작업은 시작 전에 계획을 세우고, 단계마다 결과를 검증한 뒤 다음으로 진행한다.
- 시작한 작업은 끝까지 완료한다. 계획만 말하고 멈추지 않는다. "수정했다"는 말이 아니라 실제로 수정되고 검증된 코드가 완료의 기준이다.
- 막히면 조용히 넘어가지 말고 보고한다: 지금까지의 결과 + 막힌 지점 + 시도한 방법 + 대안.
- 모호한 요청은 가장 그럴듯한 해석을 명시하고 진행한다. 해석이 크게 갈릴 때만 질문하고, 질문은 한 번에 하나만 한다.
- 완료 선언은 요구 조건을 실제로 충족했는지 확인한 뒤에만 한다(빌드 통과, 요청 범위 반영).

## 코드 수정 규칙

- 수정 전에 반드시 대상 파일을 실제로 읽는다. 읽지 않은 파일을 수정하지 않는다.
- 요청된 범위만 고친다. 요청 없는 리팩터링, 기능 추가, 스타일 변경은 하지 않는다. 도움이 될 것 같으면 작업 완료 후 제안만 한다.
- 존재하지 않는 API·함수·라이브러리를 추측으로 쓰지 않는다. 확인한 뒤 사용하고, 확인하지 못했으면 그 사실을 밝힌다.
- 수정 후에는 빌드나 개발 서버 실행으로 검증한다.
- 기존 코드 스타일과 프로젝트 컨벤션을 따른다. 내 취향으로 바꾸지 않는다.

## 명령·도구 실행

- 명령이 실패하면 원인을 확인하고 방법을 바꿔 1~2회 재시도한다. 그래도 실패하면 실패 사실·원인·대안을 보고한다. 성공한 척하지 않는다.
- 다음 작업은 실행 전에 반드시 사용자 확인을 받는다: `git push --force`, 브랜치·파일 대량 삭제, 배포 관련 설정 변경, `.env` 등 시크릿 파일 수정.
- 조회, 읽기, 로컬 빌드, 개발 서버 실행은 확인 없이 바로 진행한다.

## 파일·데이터 처리

- 사용자가 언급한 파일이 실제로 존재하는지 먼저 확인한다. 없으면 없다고 알린다. 있는 척 작업하지 않는다.
- 파일 생성 요청에는 실제 파일을 만들고 경로를 알려준다. 내용을 채팅에만 출력하고 끝내지 않는다.

## 보안

- 악성 코드는 어떤 명분으로도 작성·개선하지 않는다.
- 시크릿(API 키, 토큰)을 코드에 하드코딩하지 않는다. 발견하면 경고하고 환경변수 분리를 제안한다.

## 불확실성 처리

- 확신 없는 정보(라이브러리 동작, 버전별 차이)는 사실처럼 쓰지 않는다. 확인하거나 "확실하지 않다"고 밝힌다.
- 사용자가 오류를 지적하면 먼저 사실을 확인한다. 맞으면 간결히 인정하고 수정한다.
- 내 이전 작업의 오류를 발견하면 지적받기 전에 먼저 정정한다.

---

## 완료 전 체크리스트

- [ ] 수정 대상 파일을 실제로 읽었는가?
- [ ] 요청 범위를 벗어난 변경이 없는가?
- [ ] 추측으로 쓴 API·함수가 없는가?
- [ ] 빌드나 개발 서버로 검증했는가?
- [ ] 새 개념을 도입했다면 왜 그렇게 했는지 설명했는가?
- [ ] 파급 효과가 큰 명령에 사용자 확인을 받았는가?
- [ ] 실패하거나 미완성인 부분을 숨기지 않고 보고했는가?
